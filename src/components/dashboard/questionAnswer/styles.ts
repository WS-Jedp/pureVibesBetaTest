import styled from 'styled-components'

export const DashboardQuestionAnswserContainer = styled.article`
    position: relative;
    background-color: var(--color-light);
    box-shadow: -12px 0 30px 12px rgba(0,0,0, .1);
    margin: 0;

    @media (min-width: 720px) {
        max-height: initial;
    }
`